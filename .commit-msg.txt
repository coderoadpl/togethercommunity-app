feat: queue notification fan-out into a job table drained by the dispatch cron

Adds a notification-fanout-jobs table and a coalesced runner that
enqueues fan-out work instead of executing it inline on the request
path. The dispatch cron drains the queue and processes jobs in
batches, with thread-context resolution split into its own module.

Includes the accompanying migration, repository/usecase updates, and
test coverage for the new queue and runner behavior.

Claude-Session: https://claude.ai/code/session_013bdnTVuMPxwDDGkbRgWduh
