/**
 * Same-origin lesson attachment used by the demo seed. Served from
 * `apps/web/public/assets` by our own server, so the lesson player can frame it
 * inline (unlike third-party samples that block embedding via CSP frame-ancestors).
 */
export const SAMPLE_LESSON_PDF_URL = '/assets/sample-lekcja.pdf';
